import { SafeAreaView } from "react-native";

export default function Index() {
  return (
    <SafeAreaView className="flex-1 bg-white pb-10">
      {/* <Image
        source={require("@/assets/images/auth/onda.png")}
        style={{ width: 200, height: 200 }}
      /> */}
    </SafeAreaView>
  );
}
